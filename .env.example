# admin-platform 根环境变量（供 docker compose 使用）
# 使用方式：cp .env.example .env

# PostgreSQL（镜像 pgvector/pgvector:pg16）
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin123456
POSTGRES_DB=admin_platform
POSTGRES_PORT=5432

# Redis（镜像 redis:7-alpine）
REDIS_PORT=6379
